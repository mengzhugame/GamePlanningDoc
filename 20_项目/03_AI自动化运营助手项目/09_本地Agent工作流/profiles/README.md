# 运营档案

## 使用规则

运营档案是当前账号运营的工作摘要，不是永久个人信息库。

每个档案必须包含：

- 当前定位。
- 目标用户。
- 可用真实素材。
- 禁忌表达。
- 承接口。
- `source_of_truth` 来源链接。
- `last_synced`。

## 同步原则

如果知识库中的用户画像、项目资料或商业规划更新，不要自动假设运营档案已同步。

需要同步时，执行：

```text
读取 `skills/skill-sync-profile.md`，同步某个运营档案。
```
