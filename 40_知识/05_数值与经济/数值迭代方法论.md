---
type: knowledge
status: review
created: 2026-04-24
updated: 2026-05-29
source_book: 光与朽项目数值复盘 + 微信小游戏竞品体验 + 用户 2026-05 数值反思
source_page: 光与朽 BattleLog、数值策划对话、《噩梦守护战》《装备全靠转》《打个螺丝》等体验复盘
domain: 05_数值与经济
tags: [数值迭代, 难度曲线, IAA, 差一点通关, BattleLog, 休闲小游戏]
last_reviewed: 2026-05-29
review_count: 4
---

# 数值迭代方法论 2026

> 核心更新：光与朽的数值“能玩”，但没有做到“差一点就过、愿意看广告救回来”。下一款游戏的数值目标不是调平，而是设计出可解释失败、可补救广告点和可复盘数据。

## 光与朽的真实教训

| 问题 | 后果 |
| --- | --- |
| 需要手动跑几十局 | 迭代成本高，容易疲劳，样本不足时仍然凭感觉 |
| 数据采集有了，但指标不够贴近商业目标 | 能分析 TTK/DPS，却不一定能判断玩家愿不愿意看广告 |
| 数值“能玩”但不够好 | 玩家没有稳定形成“差一点就过”的心理 |
| 单局偏长 | 失败成本高，广告救助不一定有吸引力 |
| 爽感反馈不足 | 即使数值接近，也不觉得奖励值得 |

## 好数值的目标

小游戏尤其是 IAA 方向，数值不是单纯“公平”，而是让玩家产生四种感受：

| 玩家感受 | 数值设计含义 |
| --- | --- |
| 我知道自己为什么输了 | 失败原因可解释 |
| 我刚才差一点 | 失败发生在 80%-95% 进度附近 |
| 看一次广告能救回来 | 广告奖励能解决当前困境 |
| 我下次可能不用看广告 | 玩家保留掌控感，不觉得被勒索 |

## 阶段版 SOP

### 阶段 A：纸面阶段

先定义失败条件，再填数字。

| 必答问题 | 示例 |
| --- | --- |
| 玩家为什么会输 | 时间到、血量归零、槽位满、Boss 未击杀 |
| 玩家失败时看到什么 | 还差 3 秒、Boss 剩 8% 血、只差 1 格 |
| 广告能补什么 | 加时、复活、撤销、清槽、补伤害、免费刷新 |
| 一局理想时长 | 1-3 分钟 / 3-5 分钟 / 10 分钟 |
| 第一次失败应发生在什么时候 | 第 3 关、第 5 波、第 2 分钟后 |

### 阶段 B：7 天切片阶段

只验证一件事：能不能稳定制造“差一点”。

最小字段：

| 字段 | 作用 |
| --- | --- |
| level_id | 关卡或波次 |
| result | 胜利/失败 |
| fail_reason | 时间到、血量归零、槽位满等 |
| progress_at_fail | 失败时完成度 |
| time_spent | 单局时长 |
| retry_count | 玩家是否愿意再来 |
| ad_offer_shown | 是否出现救助 |
| ad_watched | 是否看广告 |
| result_after_ad | 看广告后是否过关 |

### 阶段 C：Demo 阶段

开始建立可复盘数据模型。

| 数据量 | 用途 |
| --- | --- |
| 自测 10-20 局 | 建立初始难度锚点 |
| 每次参数调整 5 局以上 | 验证方向，不做最终判断 |
| 陌生玩家 20 人以上 | 看失败原因是否和自测一致 |
| 上线真实数据 | 决定是否继续扩张 |

## 调参优先级

当玩家说“太难”时，不要第一反应改血量。

按顺序检查：

1. 教学是否让玩家知道目标。
2. 失败原因是否可见。
3. 操作反馈是否让玩家相信自己有掌控。
4. 广告奖励是否能解决当前失败。
5. 关卡节奏是否过长。
6. 最后才调怪物血量、时间、伤害、价格等基础数值。

## 休闲游戏广告点设计

| 失败类型 | 好广告奖励 | 坏广告奖励 |
| --- | --- | --- |
| 时间差一点 | 加 10 秒、冻结、减速 | 给金币但本局过不了 |
| Boss 血量差一点 | 临时伤害、复活、护盾 | 给随机道具且不解决 Boss |
| 空间差一点 | 多 1 格、撤销、重排 | 给无关资源 |
| 资源差一点 | 补差额、免费刷新 | 给远期成长货币 |
| 操作失误 | 撤销一步、重试当前小段 | 直接重开 |

## Roguelite / 战斗数值保留规则

光与朽里的部分战斗数值经验仍然有效，但只能作为战斗类项目的子规则。

| 规则 | 说明 |
| --- | --- |
| 不用固定伤害对抗乘法成长 | 怪物血量倍率成长时，技能也要跟攻击力或成长值挂钩 |
| 每章节单独建模型 | 不同怪物构成会让旧模型失效 |
| 出场时序用 DPS 数据验证 | 先算清场时刻，再设计炮手/坦克/分裂怪出场 |
| 流派前期不能惩罚过重 | Lv1 体验不能比不选还差 |

## 判断标准

| 现象 | 结论 |
| --- | --- |
| 失败进度常低于 50% | 太难、教学失败或节奏过快 |
| 失败进度在 80%-95%，广告观看率低 | 奖励不够诱人或玩家不相信能救回来 |
| 广告后仍大量失败 | 广告奖励设计错了 |
| 不看广告也轻松过 | 广告点不存在 |
| 玩家连续重试但不看广告 | 失败可接受，但广告价值不足 |

## 核心原则

- 能玩不等于好玩。
- 平衡不等于赚钱。
- 广告不是惩罚玩家，而是在“差一点”时给玩家一次合理补救。
- 下一款游戏从第一天就要记录失败原因和广告后的结果。

## 来源: `10_流水/历史聊天/Google-光与朽数值策划_2026-04-03.md` · 提取日期 2026-05-22

## BattleLog 先把指标定义清楚，再谈调参

数值复盘不能只看“过没过”和平均 DPS。尤其是有爆发、反射、连锁、溢出伤害的游戏，指标定义错了会把问题藏起来。

建议最小字段：

| 字段 | 定义 |
| --- | --- |
| Wave | 当前波次 |
| Build | 当前主要流派/关键技能组合 |
| Level | 玩家等级或升级次数 |
| Result | 胜利、失败、超时、Boss 未击杀等 |
| Time | 波次耗时或整局耗时 |
| PeakDPS | 1 秒时间桶内的最高 DPS，不是整波平均 |
| EnemyTotalHP | 本波敌人理论总血量 |
| PlayerTotalDmg | 玩家造成总伤害 |
| TankDmgRatio | 坦克/肉盾吸收伤害占比 |
| MaxExplosionDmg | 单次爆炸或连锁峰值 |

`Overkill` 必须按每次命中计算：`本次伤害 - 命中前剩余血量`，不能用整波总伤害减总血量。否则爆炸、暴击和溢出会把真实击杀效率污染成虚假的超高输出。

## 数值诊断顺序：先定位层级，再改参数

遇到“后期打不动”或“某流派爆炸”时，不要第一反应改怪物血量。更稳的顺序是：

1. 判断问题属于系统失控、单技能过强、成长断层还是关卡节奏。
2. 对比玩家成长曲线和怪物 EHP 曲线是否同阶。
3. 查看 PeakDPS、TankDmgRatio、MaxExplosionDmg 和 Overkill，确定是输出不足、肉盾吃伤、还是连锁崩坏。
4. 只改一层：技能倍率、冷却、怪物构成、经验供给或波次长度。
5. 用同一组日志字段复测，不凭一局手感定结论。

调参的核心不是把数值压平，而是让“爆发可控、失败可解释、广告点可设计”。

## 来源: `10_流水/Openclaw知识库文件/findings.md` · 提取日期 2026-05-26

## 暴击、攻速和多段伤害要统一折算到 EffectiveDPS

暴击、攻速、多段伤害、命中率和覆盖率如果各算各的，很容易把构筑强度看错。更稳的工作尺是先统一到 EffectiveDPS，再看 TTK、溢伤、稳定性和触发收益。

基础公式：

```text
EffectiveDPS =
每次攻击期望伤害
* 每秒有效攻击次数
* 命中率
* 占空比
* 覆盖率
```

暴击期望增益优先写成：

```text
CritExpectedGain = 1 + CritChance * (CritMultiplier - 1)
```

多段伤害不能只看总倍率，要拆成：

| 维度 | 为什么要拆 |
| --- | --- |
| 总倍率 | 决定理论上限 |
| 段数 | 影响防御减免、命中次数和反馈密度 |
| 每段防御损耗 | 按段减防时，多段可能显著吃亏 |
| 溢伤浪费 | 高频小段更容易把伤害打在已死目标上 |
| 触发收益 | 多段可能更容易触发暴击、特效或 on-hit |

攻速也要区分面板攻速和有效攻速。有效攻速要扣掉转向、停火、换轨、过热、首发延迟、目标丢失等战斗现场损耗。否则高攻速武器会在 Excel 里很强，在实战里反而浪费。

如果暴击或触发率直接跟攻速相乘，容易形成失控乘法。需要时可以改用按秒触发率、PPM 或 proc coefficient，削掉高攻速天然碾压低攻速的一层耦合。

## 来源: `10_流水/Openclaw知识库文件/game_balance_numerical_design.md` · 提取日期 2026-05-26

## 塔防数值先对齐击杀时间和敌人行进时间

塔防 / Roguelike 的早期数值锚点，不是先追求所有单位强度“看起来合理”，而是先让玩家主要输出的击杀时间和敌人从刷出到威胁点的行进时间对齐。只有这两个时间尺度接近，玩家才会感觉自己是在补短板和做取舍，而不是被血量表碾压。

基础公式先保持朴素：

```text
upgradeCost = expBase + (level - 1) * expGrowth
damagePerTick = baseDPS * tickRate
killTime = enemyHealth / (baseDPS * skillMultiplier)
```

波次调参时优先看 4 个指标：

| 指标 | 用途 |
| --- | --- |
| KillTime | 判断当前输出是否能在敌人越线前解决问题 |
| EnemyTravelTime | 判断玩家还有多少反应和补救窗口 |
| SkillPickRate | 判断某流派是不是被数值逼成唯一答案 |
| DeathCause | 区分输出不足、控场不足、经济断档还是题型读不懂 |

Roguelike 数值还要额外守住三条边界：

- DPS 等价只是起点，操作难、覆盖窄、成型慢的路线应获得补偿。
- 高风险路线要有更高上限，但不能让失败完全无收益。
- 单体、范围、控制和经济路线要分别有能验证自己的波次，不要只用一个总 DPS Check 评价所有 Build。

早期最实用的表不是复杂模拟器，而是敌人 HP 曲线、玩家 DPS 曲线和关键波次 TTK 表。先确认“第几波开始打不动”和“为什么打不动”，再改血量、经验、奖励或技能倍率。

## 来源: `10_流水/Openclaw知识库文件/game_numerical_design.md` · 提取日期 2026-05-27

## 数值表要同时看波次压力和 Build 强度

塔防 Roguelike 的数值迭代至少要有两张表：一张看波次压力，一张看 Build 强度。只看波次会误判流派差异，只看 Build 会忽略第几波突然变成生死线。

波次压力表的最低字段：

| 字段 | 用途 |
| --- | --- |
| wave | 时间轴 |
| enemy_count | 本波规模 |
| enemy_hp | 单体血量或 EHP |
| spawn_interval | 生成节奏 |
| required_dps | `enemy_count * enemy_hp / 可处理时间` |
| actual_dps | 实测或模拟玩家 DPS |
| pressure_ratio | `required_dps / actual_dps` |

`pressure_ratio` 不应一直平。更好的曲线是在 0.8-1.2 附近波动，并在关键波次制造短峰值；如果某一波突然超过太多，要确认这是教学、构筑、敌人题型还是纯数值断崖。

Build 强度表要看组合增益，而不是只加总技能描述：

```text
base_dps = attack * attack_frequency
actual_dps = base_dps * (1 + bonus_a) * (1 + bonus_b) * ...
build_power = actual_dps / base_dps
```

单个技能可以稳定但不出彩，组合技能才负责质变。一个可用的粗分层：

| 强度 | 体感 |
| --- | --- |
| 3-5 倍基础 DPS | T1，可能过强，需要看成型成本和可读性 |
| 2-3 倍基础 DPS | T2，理想主流构筑 |
| 1.5-2 倍基础 DPS | T3，可用但需要补偿操作、安全或控制价值 |
| 低于 1.5 倍 | T4，容易被玩家抛弃 |

当某个通关 Build 和失败 Build 账面 DPS 只差 10%-15%，却决定生死时，不要只看总 DPS。要拆第几波、什么敌人、什么题型造成失败：控制流可能缺的不是总伤害，而是对关键波次的爆发、破盾、清杂或 Boss 前补救窗口。

## 来源: `10_流水/Openclaw知识库文件/numerical_design_best_practices.md` · 提取日期 2026-05-29

## 数值建模要同时看均值、方差、分位数和断点

这份数值最佳实践的长期价值不是某一张具体表，而是一套“别被平均数骗了”的建模纪律。数值策划至少要同时看四类指标：

| 指标 | 解决什么问题 | 示例 |
| --- | --- | --- |
| 均值 | 长期资源、伤害或出货是否符合目标 | `E[X]`、平均 DPS、平均毕业时长 |
| 方差 | 短期体感是否过欧/过非 | PRD 压缩暴击或关键掉落触发间隔 |
| 分位数 | 大多数玩家真实会经历什么 | `Q90/Q95` 掉落次数、毕业时长 |
| 断点 | 小幅数值变化是否跨过体感阈值 | `ShotsToKill = ceil(EnemyHP / HitDamage)` |

如果只看均值，会误判概率和掉落；如果只看 DPS，会忽略击杀断点；如果只看面板战力，会忽略流派成型成本、覆盖率和操作难度。

## PRD、保底和权重池要分工使用

概率系统先分清三件事：

| 工具 | 适合解决 | 不适合 |
| --- | --- | --- |
| 真随机 | 保留惊喜、终局稀有物、低频炫耀掉落 | 高频核心体验 |
| PRD | 高频暴击、异常、关键类别触发，压缩短期方差 | 所有稀有物都平滑，削弱惊喜 |
| 软/硬保底 | 低频高价值掉落、抽卡、核心碎片获取 | 公开概率未核清的付费机制 |
| 权重池 | 控制掉落槽、子池、不同物品比例 | 不计算真实概率链 |

关键公式：

```text
PRD: p_n = min(1, C * n)，C 需要反解，不能直接等于目标概率 p
目标物真实概率: p_i = p_slot * w_i / Σw
单件期望: E_i = 1 / p_i
至少出 1 次: P = 1 - (1 - p_i)^n
```

设计顺序应是：

```text
目标体验 -> 目标期望次数 -> Q90/Q95 -> 概率/权重/保底 -> Monte Carlo 验证
```

不要反过来先拍一个“金装 1.5%”再解释它为什么合理。

## 战力评估看边际收益和三轴评分

战力模型不是做一个好看的总战力数字，而是把不同属性放到同一把尺子上，判断哪个词条在当前状态下真实更值。

基础输出可先写成：

```text
DPS = ATK * APS * CritFactor * SkillFactor * EnemyModifier
CritFactor = 1 + CritRate * CritDamageBonus
Value(X) = ΔDPS / 当前DPS
```

生存属性统一到有效生命：

```text
DR = DEF / (DEF + K)
EHP = HP / (1 - DR)
SurvivalScore = EHP + HPS * 预期存活时长
```

更适合 Roguelike / 塔防的结构是三轴，而不是一个总数字：

| 轴 | 看什么 |
| --- | --- |
| 输出轴 | DPS、AoE 覆盖、射程、占空比、命中率 |
| 生存轴 | EHP、回复、闪避、减速/控制带来的少吃伤 |
| 功能轴 | 控场、资源收益、Build 协同、稳定性 |

三选一估值可以先粗写为：

```text
PickValue = (ΔOutput + λ*ΔSurvival + μ*ΔUtility) * SynergyFactor
```

这能避免把所有奖励都调成“攻击 +X%”，也能解释为什么某些低 DPS 选项在关键波次仍然有价值。

## 成长曲线设计的是时间感，不是等级表

经验曲线必须和时间、波次、奖励价值一起算。只看 `ExpToNext` 没意义，至少要有：

| 字段 | 用途 |
| --- | --- |
| `ExpToNext` | 升级需求 |
| `ExpIncomePerMinute` | 经验产出速度 |
| `TimeToNext` | 玩家多久获得一次正反馈 |
| `PowerGainPerLevel` | 这一级值不值 |
| `WaveSync` | 玩家当前战力 / 当前波次需求 |
| `BuildMilestone` | 核心流派第几级成型 |

主曲线优先用分段多项式或低阶幂函数，不要迷信一条指数公式。一个可复用结构是：

```text
前期：线性/低阶幂函数，保证反馈密度
中期：多项式，制造构筑分化
后期：缓冲或收敛，防滚雪球和毕业过快
```

公式最后必须回到模拟和试玩：是否 30-45 秒内有一次正反馈，是否存在 2 分钟以上成长空窗，是否出现“升了级但没感觉”的空心奖励。

## 掉落、广告和商店要共用毕业进度尺

只要存在掉落池、重复分解、商店兑换和广告补偿，就必须把它们折算成同一套“毕业进度价值”。否则广告可能打穿经济，商店可能变成唯一最优，重复物也可能变成纯垃圾。

可落表字段：

| 字段 | 用途 |
| --- | --- |
| `SlotDropProb` | 是否进入某掉落槽 |
| `ItemWeight` | 目标物在槽内权重 |
| `RealItemProb` | 真实单次概率 |
| `E_Item` | 单件期望次数 |
| `N50/N90/Q95` | 多数玩家体感区间 |
| `DuplicateShard` | 重复物回收价值 |
| `CraftCost` | 定向合成成本 |
| `AdCompensationValue` | 广告等效尝试次数 |
| `HoursToGraduate` | 自然毕业时长 |

对中盘 Build 成型件，不能全靠大池赌博。更稳的结构是：基础掉落 + 定向兑换 + 重复分解补进度。终局炫耀物可以保留更高方差，但核心流派件必须有可解释的追赶路径。

## 来源: `10_流水/Openclaw知识库文件/numerical_design_plan.md` · 提取日期 2026-05-29

## 数值工作分三阶段：局内 TTK、局外经济、IAA 点位

《光与朽》旧数值计划可沉淀为一条通用顺序：

| 阶段 | 目标 | 产物 |
| --- | --- | --- |
| 局内战斗 | 拉平玩家成长和怪物膨胀，保证爽感与压力动态平衡 | 波次压力表、玩家基础数值、怪物数值、经验曲线、TTK 模型 |
| 局外经济 | 让玩家有持续刷局动力，但不过快毕业 | 单局资源产出、科技树消耗、装备/碎片掉落、毕业时长 |
| IAA 商业化 | 在不恶心玩家的前提下，让广告解决真实卡点 | 复活、刷新、结算翻倍、补资源、广告频次和收益系数 |

这个顺序不能乱。没有局内 TTK，就不知道“差一点”发生在哪里；没有局外经济，就不知道广告奖励是否打穿成长；没有失败数据，就不能判断广告是补救还是勒索。

最小数值闭环应是：

```text
波次压力 -> 玩家成长 -> 失败点 -> 广告补救 -> 广告后结果 -> 局外成长回流
```

因此数值表不应分散成互不相干的“怪物表、经验表、广告表”。它们要共享同一套时间、战力、资源和失败原因字段。
